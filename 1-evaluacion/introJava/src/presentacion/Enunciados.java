package presentacion;

public class Enunciados {

	// Ej Intro
	/*
	 * 1 – Hacer un programa en el que pongamos 3 números enteros, los sume e
	 * imprima el resultado por pantalla
	 * 
	 * 2 – Hacer un programa en el que pongamos una variable con un saludo y otra
	 * con nuestro nombre e imprima por pantalla la frase completa
	 * 
	 * 3 – Hacer un programa en el que pongamos 4 notas de un alumno y su nombre y
	 * nos saque por pantalla una frase de este tipo: “El promedio de notas de
	 * (nombre del alumno) es de (promedio de notas)”
	 */

	// Ej Scanner
	/*
	 * 1 – Hacer un programa en el que nos pida por pantalla nuestro nombre y
	 * apellido, lo introduzcamos por teclado y nos saque por pantalla “Hola
	 * (nombre)”
	 * 
	 * 2 – Hacer un programa en el que nos pida por pantalla un número, lo
	 * introduzcamos, luego otro, lo introduzcamos y luego otro más, lo
	 * introduzcamos y, finalmente, nos saque por pantalla la suma de los tres
	 * números*
	 */

	// Ej Operadores Logicos
	/*
	 * 1 – Dadas las variables de tipo int con valores A = 5, B = 3, C = -12 indicar
	 * si la evaluación de estas expresiones daría como resultado verdadero o falso:
	 * a) A > 3 
	 * b) A > C 
	 * c) A < C 
	 * d) B < C 
	 * e) B != C 
	 * f) A == 3 
	 * g) A * B == 15 
	 * h) A * B == -30 
	 * i) C / B < A 
	 * j) C / B == -10 
	 * k) C / B == -4 
	 * l) A + B + C == 5 
	 * m)(A+B == 8) && (A-B == 2) 
	 * n) (A+B == 8) || (A-B == 6) o) A > 3 && B > 3 && C < 3 
	 * p) A > 3 && B >= 3 && C < -3
	 * 
	 */
	
	// Ej Operadores Logicos
	/*
	* 
	* 
	* 
	* */

}
